apply plugin: 'java'

dependencies {
    compile fileTree(dir: 'libs', include: ['*.jar'])
}

// greendao generator ==============================
dependencies {
    compile 'de.greenrobot:greendao-generator:2.1.0'
}
// =================================================